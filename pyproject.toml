[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentified-opencaptchaworld"
version = "1.0.0"
description = "Agent-based benchmark for OpenCaptchaWorld interactive CAPTCHA solving"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.3.5",
    "google-adk>=1.14.1",
    "google-genai>=1.36.0",
    "jinja2>=3.1.6",
    "pydantic>=2.11.9",
    "python-dotenv>=1.1.1",
    "uvicorn>=0.35.0",
]

[project.scripts]
agentbeats-run = "agentbeats.run_scenario:main"

[tool.uv]
package = true
dev-dependencies = [
    "mypy>=1.18.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agentbeats"]
